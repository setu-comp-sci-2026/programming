# Debugging and reflection

## Debugging 1

What is wrong?

~~~python
age = int(input("Age: "))

if age >= 18 and <= 65:
    print("Working age range")
~~~

Fix the condition.

## Debugging 2

The program should print `Distinction` for 70 or above.

~~~python
mark = int(input("Mark: "))

if mark >= 40:
    print("Pass")
elif mark >= 70:
    print("Distinction")
else:
    print("Fail")
~~~

It runs, but the logic is wrong.

Fix it.

## Debugging 3

The discount applies to students **or** customers aged 65+.

~~~python
age = int(input("Age: "))
student = input("Student? yes/no: ").lower()

if age >= 65 and student == "yes":
    print("Discount")
else:
    print("No discount")
~~~

Fix the logical operator.

## Debugging 4

The valid range is 0 through 100 inclusive.

~~~python
if mark > 0 and mark < 100:
    print("Valid")
~~~

Fix the boundary conditions.

## Reflection

Answer in your own words:

1. What is the difference between `if` and `if-else`?
2. When would you use `elif`?
3. What does `and` mean?
4. What does `or` mean?
5. Why is indentation important in Python?
6. Why should boundary values be tested?
7. When might separate `if` statements be better than an `if-elif-else` chain?

# Lab - Selection in Practice

In this lab, you will build on simple `if` and `if-else` statements.

You will use:

- `if-elif-else`
- multiple conditions
- `and`
- `or`
- `not`
- chained comparisons
- nested selection
- input validation
- selection inside small programs

Create a folder called:

~~~
selection-in-practice
~~~

Open it in VS Code.

Create a separate Python file for each step.

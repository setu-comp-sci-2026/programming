# 6. Python Program
TODO either remove or fully use numbering

Develop your program using Python.

Your program must include all of the following.

## 6.1 Input and output

Your program must:

- display a suitable welcome message
- ask the user for at least five pieces of information
- store the information in appropriately named variables
- use suitable data types
- use type conversion where necessary
- display a clear final recommendation or result

For example:

```python
name = input("Enter your name: ")
age = int(input("Enter your age: "))
budget = float(input("Enter your budget: €"))

```

---

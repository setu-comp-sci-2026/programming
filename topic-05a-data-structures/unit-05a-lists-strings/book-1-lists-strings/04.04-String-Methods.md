# String methods

Create a file called **04_string_methods.py**.

~~~python
name = "  siobhan roche  "

print(name.strip())
print(name.upper())
print(name.title())
~~~

Run your code.

## User input cleanup

Ask the user to enter their name. Remove extra spaces and display it in title case.

~~~python
name = input("Enter your name: ")
name = name.strip().title()
print("Hello", name)
~~~

## Splitting and joining

~~~python
sentence = "Python is a useful language"
words = sentence.split()
print(words)

new_sentence = "-".join(words)
print(new_sentence)
~~~

## Task

Ask the user to enter a sentence. Print:

- the sentence in uppercase
- the number of characters
- the list of words
- the number of words

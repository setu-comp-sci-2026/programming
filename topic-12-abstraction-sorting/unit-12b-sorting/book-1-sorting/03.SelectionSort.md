# Step 3 — Implementing Selection Sort

```python
def selection_sort(lst: list) -> list:
    data = lst.copy()
    n = len(data)
    for i in range(n - 1):
        min_idx = i
        for j in range(i + 1, n):
            if data[j] < data[min_idx]:
                min_idx = j
        if min_idx != i:
            data[i], data[min_idx] = data[min_idx], data[i]
    return data

numbers = [64, 25, 12, 22, 11]
print(selection_sort(numbers))
# Expected: [11, 12, 22, 25, 64]
```

## Compare both algorithms

```python
import random
test_data = random.sample(range(1000), 20)

print("Bubble:", bubble_sort(test_data))
print("Selection:", selection_sort(test_data))
print("Built-in:", sorted(test_data))
# All three should produce the same result
```

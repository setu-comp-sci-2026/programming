# Defining Boolean variables
isOpen = True
isSet = False

# Printing the contents of the Boolean variables to the terminal
print("The value of isOpen is: " + str(isOpen))
print("The value of isSet is: " + str(isSet))

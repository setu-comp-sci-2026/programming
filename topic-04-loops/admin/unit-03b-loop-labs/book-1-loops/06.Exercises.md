# Exercises

Complete these exercises to practise your loop skills.

## Exercise 1 — FizzBuzz

Print numbers from 1 to 50. But:
- If the number is divisible by 3, print `Fizz` instead
- If the number is divisible by 5, print `Buzz` instead
- If divisible by both 3 and 5, print `FizzBuzz`

```python
for i in range(1, 51):
    # Your code here
    pass
```

Expected output begins: `1 2 Fizz 4 Buzz Fizz 7 8 Fizz Buzz 11 Fizz 13 14 FizzBuzz ...`

## Exercise 2 — Sum of Digits

Ask the user for a positive integer. Compute the sum of its digits.

Example: `1234` → `1 + 2 + 3 + 4 = 10`

```python
number = int(input("Enter a positive integer: "))
# Hint: convert to string, loop over characters
digit_sum = 0
# Your code here
print(f"Sum of digits: {digit_sum}")
```

## Exercise 3 — Number Pyramid

Ask the user for a number `n`. Print a right-aligned number pyramid up to `n` rows.

For `n = 4`:
```
   1
  12
 123
1234
```

## Exercise 4 — Word Statistics

Ask the user to enter a sentence. Print:
- Number of words
- Number of characters (excluding spaces)
- Number of vowels
- The longest word

```python
sentence = input("Enter a sentence: ")
words = sentence.split()
# Your code here
```

# Looping Through Strings

A string is a sequence of characters.

~~~python
name = input("Enter your name: ")

for letter in name:
    print(letter)
~~~

Run the program.

What happens?

Modify the program so that it counts the number of vowels in the name.
